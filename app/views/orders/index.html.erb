<h1>Listing orders</h1>
Excelからコピペしてください<br>
(書体　L1 L2 L3 の4カラム)
<%= form_tag({:action => 'import_tsv'}) do |f| %>
  <div class="field">
    <%= text_area_tag 'tsv', nil, :rows=>10, :cols=>80 %>
  </div>
  <div class="field">
    プロジェクト：
    <%= select_tag 'project',options_from_collection_for_select((Project.all), 'id', 'name') %>
  </div>

  <div class="actions">
    <%= submit_tag 'オーダー登録' %>
  </div>
<% end %>


<br>
オーダー履歴 
<%= link_to 'New Order', new_order_path %> | 
<%= link_to 'CREATE SVG', :action=>'to_svg' %>

<table border=1>
  <tr>
    <th>L1</th>
    <th>L2</th>
    <th>L3</th>
    <th>Fontset</th>
    <th>Project</th>
    <th>Done</th>
    <th>Lot</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.l1 %></td>
    <td><%= order.l2 %></td>
    <td><%= order.l3 %></td>
    <td><%= order.fontset.name if order.fontset %></td>
    <td><%= order.project.name if order.project %></td>
    <td><%= order.done %></td>
    <td><%= order.lot %></td>
    <td><%= link_to 'Show', order %></td>
    <td><%= link_to 'Edit', edit_order_path(order) %></td>
    <td><%= link_to 'Destroy', order, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />
| 
<%= link_to 'Projects', url_for(:controller=>'projects', :action=>'index')%> | 
<%= link_to 'FontSets', url_for(:controller=>'fontsets', :action=>'index')%> | 
<%= link_to 'Fonts', url_for(:controller=>'fonts', :action=>'index')%> | 
